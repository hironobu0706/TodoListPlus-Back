<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.LoginMapper">
	<select id="loginAuth" resultType="com.example.demo.model.LoginModel">
		select
          user_id
          ,user_name
		from login
		where
		  mail_Address = #{mailAddress}
		  AND password = #{password}
	</select>
	<insert id="createCustomer">
		insert into login (
		mail_address,
		password,
		user_name,
		created_by,
		updated_by)
		values(
		#{mail_address},
		#{password},
		#{user_name},
		#{created_by},
		#{updated_by});
	</insert>
</mapper>